# 📱 دليل اختبار Postman - شرح تفصيلي بالعربي

## 🌐 رابط السيرفر الأساسي
```
http://213.210.20.206:9000
```

---

# 🔐 تسجيل الدخول للراكب

## 1️⃣ إرسال كود التحقق (OTP)

### 📍 Endpoint:
```
POST /api/v1/auth/rider/phone
```

### 📝 الوظيفة:
هذا الـ endpoint بيرسل كود تحقق (OTP) على رقم تليفون الراكب عشان يسجل دخول

### 📥 البيانات المطلوبة (Body):
```json
{
  "phone": "+201234567890"
}
```

#### شرح الحقول:
- **`phone`** (رقم التليفون): 
  - لازم يكون بالصيغة الدولية (يبدأ بـ `+` ورمز الدولة)
  - مثال: `+201234567890` (مصر)
  - مثال: `+966512345678` (السعودية)

### 📤 الرد المتوقع:
```json
{
  "status": "success",
  "message": "OTP sent to +201234567890"
}
```

#### شرح الرد:
- **`status`**: حالة الطلب (success = نجح)
- **`message`**: رسالة توضيحية إن الكود اتبعت

---

## 2️⃣ التحقق من الكود وتسجيل الدخول

### 📍 Endpoint:
```
POST /api/v1/auth/rider/verify-otp
```

### 📝 الوظيفة:
بعد ما الراكب يستلم الكود على تليفونه، بيدخله هنا عشان يتحقق منه ويسجل دخول

### 📥 البيانات المطلوبة (Body):
```json
{
  "phone": "+201234567890",
  "code": "123456"
}
```

#### شرح الحقول:
- **`phone`** (رقم التليفون): نفس الرقم اللي بعتنا عليه الكود
- **`code`** (كود التحقق): الكود المكون من 6 أرقام اللي وصل على التليفون

### 📤 الرد المتوقع:
```json
{
  "status": "success",
  "message": "Phone verified successfully",
  "isFirstTime": true,
  "data": {
    "_id": "674567890abcdef123456789",
    "phone": "+201234567890",
    "role": "user",
    "status": "active",
    "isPhoneVerified": true
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

#### شرح الرد:
- **`isFirstTime`**: هل ده أول مرة المستخدم يسجل دخول؟ (true = أول مرة)
- **`data`**: بيانات المستخدم
  - **`_id`**: رقم تعريف المستخدم في قاعدة البيانات
  - **`phone`**: رقم التليفون
  - **`role`**: نوع المستخدم (`user` = راكب، `driver` = سواق)
  - **`status`**: حالة الحساب (`active` = نشط، `pending` = في انتظار الموافقة)
  - **`isPhoneVerified`**: هل الرقم متحقق منه؟
- **`token`**: مفتاح الدخول (لازم تحفظه وتستخدمه في كل الطلبات التالية)

### ⚠️ مهم جداً:
احفظ الـ **token** ده! هتحتاجه في كل طلب تاني. حطه في الـ Headers كده:
```
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

# 🏠 الصفحة الرئيسية - طلب رحلة

## 3️⃣ عرض العناوين المحفوظة

### 📍 Endpoint:
```
GET /api/v1/passenger/addresses
```

### 📝 الوظيفة:
يعرض كل العناوين اللي الراكب حفظها قبل كده (البيت، الشغل، أماكن مفضلة)

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
```

### 📤 الرد المتوقع:
```json
{
  "status": "success",
  "results": 2,
  "data": [
    {
      "label": "home",
      "address": "22 شارع الثورة، مدينة نصر",
      "coordinates": {
        "latitude": 30.0444,
        "longitude": 31.2357
      }
    },
    {
      "label": "work",
      "address": "12 شارع المرغني، مصر الجديدة",
      "coordinates": {
        "latitude": 30.0876,
        "longitude": 31.3421
      }
    }
  ]
}
```

#### شرح الرد:
- **`results`**: عدد العناوين المحفوظة
- **`data`**: قائمة العناوين
  - **`label`**: نوع العنوان (`home` = البيت، `work` = الشغل، `favorite` = مفضل)
  - **`address`**: العنوان بالكامل (نص)
  - **`coordinates`**: الإحداثيات الجغرافية
    - **`latitude`**: خط العرض
    - **`longitude`**: خط الطول

---

## 4️⃣ البحث عن سواقين قريبين

### 📍 Endpoint:
```
GET /api/v1/passenger/drivers/nearby?latitude=30.0444&longitude=31.2357&radius=5
```

### 📝 الوظيفة:
يبحث عن السواقين المتاحين (Online) في منطقة قريبة من موقع الراكب

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
```

### 📥 Parameters (في الـ URL):
- **`latitude`** (خط العرض): موقع الراكب الحالي - خط العرض
- **`longitude`** (خط الطول): موقع الراكب الحالي - خط الطول
- **`radius`** (نطاق البحث): المسافة بالكيلومتر (مثلاً 5 = ابحث في دائرة نصف قطرها 5 كم)

### 📤 الرد المتوقع:
```json
{
  "status": "success",
  "results": 5,
  "data": [
    {
      "_id": "674567890abcdef123456789",
      "fullName": "أحمد محمد",
      "profileImg": "http://213.210.20.206:9000/uploads/users/driver1.jpg",
      "rating": 4.8,
      "currentLocation": {
        "latitude": 30.0450,
        "longitude": 31.2360
      },
      "distance": 0.8
    }
  ]
}
```

#### شرح الرد:
- **`results`**: عدد السواقين المتاحين
- **`data`**: قائمة السواقين
  - **`fullName`**: اسم السواق
  - **`profileImg`**: صورة السواق
  - **`rating`**: تقييم السواق (من 5)
  - **`currentLocation`**: موقع السواق الحالي
  - **`distance`**: المسافة بين الراكب والسواق (بالكيلومتر)

---

## 5️⃣ طلب رحلة

### 📍 Endpoint:
```
POST /api/v1/passenger/rides/request
```

### 📝 الوظيفة:
الراكب يطلب رحلة جديدة من مكان لمكان

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json
```

### 📥 البيانات المطلوبة (Body):
```json
{
  "pickupAddress": "22 شارع الثورة، مدينة نصر",
  "pickupLatitude": 30.0444,
  "pickupLongitude": 31.2357,
  "dropoffAddress": "12 شارع المرغني، مصر الجديدة",
  "dropoffLatitude": 30.0876,
  "dropoffLongitude": 31.3421,
  "vehicleType": "economy",
  "paymentMethod": "cash",
  "stops": [],
  "voucherCode": "SAVE20",
  "useWallet": false
}
```

#### شرح الحقول:
- **`pickupAddress`** (عنوان الصعود): المكان اللي السواق هيجي يستلم الراكب منه (نص)
- **`pickupLatitude`** (خط عرض الصعود): الإحداثيات - خط العرض
- **`pickupLongitude`** (خط طول الصعود): الإحداثيات - خط الطول
- **`dropoffAddress`** (عنوان النزول): المكان اللي الراكب رايح له (نص)
- **`dropoffLatitude`** (خط عرض النزول): الإحداثيات - خط العرض
- **`dropoffLongitude`** (خط طول النزول): الإحداثيات - خط الطول
- **`vehicleType`** (نوع العربية): 
  - `economy` = اقتصادي (رخيص)
  - `comfort` = مريح (متوسط)
  - `premium` = فاخر (غالي)
- **`paymentMethod`** (طريقة الدفع):
  - `cash` = كاش
  - `wallet` = من المحفظة
- **`stops`** (محطات توقف): أماكن تانية الراكب عايز يوقف فيها في الطريق (مصفوفة)
- **`voucherCode`** (كود خصم): اختياري - لو عنده كوبون خصم
- **`useWallet`** (استخدام المحفظة): true = ادفع من المحفظة، false = لا

### 📤 الرد المتوقع:
```json
{
  "status": "success",
  "message": "Ride requested successfully. Looking for nearby drivers...",
  "data": {
    "ride": {
      "_id": "674567890abcdef123456789",
      "status": "pending",
      "distance": 8.5,
      "duration": 17,
      "vehicleType": "economy"
    },
    "pricing": {
      "baseFare": 70,
      "voucherDiscount": 20,
      "walletAmountUsed": 0,
      "finalFare": 50
    }
  }
}
```

#### شرح الرد:
- **`ride`**: بيانات الرحلة
  - **`_id`**: رقم الرحلة
  - **`status`**: حالة الرحلة (`pending` = في انتظار سواق)
  - **`distance`**: المسافة الكلية (كيلومتر)
  - **`duration`**: الوقت المتوقع (دقيقة)
- **`pricing`**: تفاصيل السعر
  - **`baseFare`**: السعر الأساسي قبل أي خصم
  - **`voucherDiscount`**: قيمة الخصم من الكوبون
  - **`walletAmountUsed`**: المبلغ المدفوع من المحفظة
  - **`finalFare`**: السعر النهائي بعد كل الخصومات

---

# 🚗 الرحلة النشطة

## 6️⃣ عرض الرحلة الحالية

### 📍 Endpoint:
```
GET /api/v1/passenger/rides/active
```

### 📝 الوظيفة:
يعرض تفاصيل الرحلة اللي الراكب فيها دلوقتي (لو موجودة)

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
```

### 📤 الرد المتوقع:
```json
{
  "status": "success",
  "data": {
    "_id": "674567890abcdef123456789",
    "status": "started",
    "driver": {
      "_id": "674567890abcdef987654321",
      "fullName": "أحمد محمد",
      "phone": "+201111111111",
      "profileImg": "http://...",
      "rating": 4.8,
      "currentLocation": {
        "latitude": 30.0500,
        "longitude": 31.2400
      },
      "driverProfile": {
        "carPhotos": ["http://..."]
      }
    },
    "pickupLocation": {
      "address": "22 شارع الثورة، مدينة نصر",
      "coordinates": {
        "latitude": 30.0444,
        "longitude": 31.2357
      }
    },
    "dropoffLocation": {
      "address": "12 شارع المرغني، مصر الجديدة",
      "coordinates": {
        "latitude": 30.0876,
        "longitude": 31.3421
      }
    },
    "distance": 8.5,
    "duration": 17,
    "finalFare": 50,
    "vehicleType": "economy"
  }
}
```

#### شرح الرد:
- **`status`**: حالة الرحلة
  - `pending` = في انتظار سواق
  - `accepted` = سواق قبل الرحلة
  - `started` = السواق في الطريق للراكب
  - `arrived` = السواق وصل
  - `completed` = الرحلة خلصت
- **`driver`**: بيانات السواق
  - **`fullName`**: اسم السواق
  - **`phone`**: رقم تليفون السواق (للتواصل)
  - **`rating`**: تقييم السواق
  - **`currentLocation`**: موقع السواق الحالي (بيتحدث لحظياً)
  - **`driverProfile.carPhotos`**: صور العربية
- **`pickupLocation`**: مكان الصعود
- **`dropoffLocation`**: مكان النزول
- **`finalFare`**: السعر النهائي

---

## 7️⃣ إلغاء الرحلة

### 📍 Endpoint:
```
POST /api/v1/passenger/rides/{rideId}/cancel
```

### 📝 الوظيفة:
الراكب يلغي الرحلة (قبل ما تبدأ أو في أي وقت)

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json
```

### 📥 البيانات المطلوبة (Body):
```json
{
  "reason": "تغيير في الخطة"
}
```

#### شرح الحقول:
- **`reason`** (سبب الإلغاء): نص اختياري - ليه الراكب ألغى الرحلة

### 📤 الرد المتوقع:
```json
{
  "status": "success",
  "message": "Ride cancelled successfully",
  "data": {
    "_id": "674567890abcdef123456789",
    "status": "cancelled",
    "cancelledBy": "passenger",
    "cancellationReason": "تغيير في الخطة"
  }
}
```

---

# ⭐ تقييم السواق

## 8️⃣ تقييم السواق بعد الرحلة

### 📍 Endpoint:
```
POST /api/v1/passenger/rides/{rideId}/rate
```

### 📝 الوظيفة:
بعد ما الرحلة تخلص، الراكب يقيّم السواق

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json
```

### 📥 البيانات المطلوبة (Body):
```json
{
  "rating": 5,
  "review": "سواق ممتاز وأخلاق عالية"
}
```

#### شرح الحقول:
- **`rating`** (التقييم): رقم من 1 إلى 5 نجوم
  - 1 = سيء جداً
  - 2 = سيء
  - 3 = متوسط
  - 4 = جيد
  - 5 = ممتاز
- **`review`** (التعليق): نص اختياري - رأي الراكب في السواق

---

# 💰 المحفظة

## 9️⃣ عرض رصيد المحفظة

### 📍 Endpoint:
```
GET /api/v1/passenger/wallet
```

### 📝 الوظيفة:
يعرض رصيد المحفظة وآخر المعاملات

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
```

### 📤 الرد المتوقع:
```json
{
  "status": "success",
  "data": {
    "balance": 250.50,
    "recentTransactions": [
      {
        "_id": "674567890abcdef123456789",
        "type": "topup",
        "amount": 100,
        "balanceBefore": 150.50,
        "balanceAfter": 250.50,
        "description": "Wallet top-up",
        "createdAt": "2025-11-27T07:00:00Z"
      },
      {
        "_id": "674567890abcdef987654321",
        "type": "ride_payment",
        "amount": -70,
        "balanceBefore": 220.50,
        "balanceAfter": 150.50,
        "ride": "674567890abcdef111111111",
        "description": "Ride payment",
        "createdAt": "2025-11-26T15:30:00Z"
      }
    ]
  }
}
```

#### شرح الرد:
- **`balance`**: الرصيد الحالي (بالجنيه)
- **`recentTransactions`**: آخر المعاملات
  - **`type`**: نوع المعاملة
    - `topup` = شحن المحفظة
    - `ride_payment` = دفع رحلة
    - `refund` = استرجاع فلوس
  - **`amount`**: المبلغ (موجب = إضافة، سالب = خصم)
  - **`balanceBefore`**: الرصيد قبل المعاملة
  - **`balanceAfter`**: الرصيد بعد المعاملة
  - **`description`**: وصف المعاملة

---

## 🔟 شحن المحفظة

### 📍 Endpoint:
```
POST /api/v1/passenger/wallet/topup
```

### 📝 الوظيفة:
الراكب يشحن محفظته بفلوس

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json
```

### 📥 البيانات المطلوبة (Body):
```json
{
  "amount": 100
}
```

#### شرح الحقول:
- **`amount`** (المبلغ): كام جنيه عايز يشحن (لازم يكون رقم موجب)

---

# 🎫 الكوبونات (أكواد الخصم)

## 1️⃣1️⃣ عرض الكوبونات المتاحة

### 📍 Endpoint:
```
GET /api/v1/passenger/vouchers
```

### 📝 الوظيفة:
يعرض كل أكواد الخصم اللي الراكب يقدر يستخدمها

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
```

### 📤 الرد المتوقع:
```json
{
  "status": "success",
  "results": 2,
  "data": [
    {
      "_id": "674567890abcdef123456789",
      "code": "SAVE20",
      "discountType": "fixed",
      "discountValue": 20,
      "minRideAmount": 50,
      "expiryDate": "2025-12-31T23:59:59Z",
      "usageLimit": 100,
      "usagePerUser": 1,
      "usedCount": 45,
      "userUsedCount": 0,
      "isActive": true
    },
    {
      "_id": "674567890abcdef987654321",
      "code": "PERCENT10",
      "discountType": "percentage",
      "discountValue": 10,
      "maxDiscount": 15,
      "minRideAmount": 30,
      "expiryDate": "2025-12-15T23:59:59Z",
      "usageLimit": 200,
      "usagePerUser": 3,
      "usedCount": 87,
      "userUsedCount": 1,
      "isActive": true
    }
  ]
}
```

#### شرح الرد:
- **`code`**: الكود اللي الراكب هيكتبه
- **`discountType`**: نوع الخصم
  - `fixed` = خصم ثابت (مثلاً 20 جنيه)
  - `percentage` = نسبة مئوية (مثلاً 10%)
- **`discountValue`**: قيمة الخصم (20 أو 10 حسب النوع)
- **`maxDiscount`**: أقصى خصم (للنسبة المئوية فقط)
- **`minRideAmount`**: أقل سعر رحلة عشان الكوبون يشتغل
- **`expiryDate`**: تاريخ انتهاء الكوبون
- **`usageLimit`**: عدد مرات الاستخدام الكلي
- **`usagePerUser`**: عدد مرات الاستخدام لكل راكب
- **`usedCount`**: كام مرة اتستخدم لحد دلوقتي
- **`userUsedCount`**: كام مرة الراكب ده استخدمه
- **`isActive`**: هل الكوبون شغال؟

---

## 1️⃣2️⃣ تطبيق كوبون خصم

### 📍 Endpoint:
```
POST /api/v1/passenger/vouchers/apply
```

### 📝 الوظيفة:
الراكب يجرب الكوبون على سعر رحلة معين عشان يشوف هيوفر كام

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json
```

### 📥 البيانات المطلوبة (Body):
```json
{
  "code": "SAVE20",
  "rideAmount": 70
}
```

#### شرح الحقول:
- **`code`**: كود الخصم
- **`rideAmount`**: سعر الرحلة الأصلي

### 📤 الرد المتوقع:
```json
{
  "status": "success",
  "message": "Voucher applied successfully",
  "data": {
    "code": "SAVE20",
    "discountType": "fixed",
    "discountValue": 20,
    "calculatedDiscount": 20,
    "originalAmount": 70,
    "finalAmount": 50
  }
}
```

#### شرح الرد:
- **`calculatedDiscount`**: الخصم الفعلي اللي اتطبق
- **`originalAmount`**: السعر الأصلي
- **`finalAmount`**: السعر بعد الخصم

---

# 👤 البروفايل

## 1️⃣3️⃣ عرض البروفايل

### 📍 Endpoint:
```
GET /api/v1/users/profile
```

### 📝 الوظيفة:
يعرض كل بيانات الراكب

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
```

---

## 1️⃣4️⃣ تحديث البروفايل

### 📍 Endpoint:
```
PUT /api/v1/users/profile
```

### 📝 الوظيفة:
الراكب يعدل بياناته (الاسم، الإيميل)

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json
```

### 📥 البيانات المطلوبة (Body):
```json
{
  "fullName": "محمد أحمد علي",
  "email": "mohamed.new@example.com"
}
```

#### شرح الحقول:
- **`fullName`**: الاسم الكامل
- **`email`**: البريد الإلكتروني

---

## 1️⃣5️⃣ تحديث صورة البروفايل

### 📍 Endpoint:
```
PUT /api/v1/users/profile
```

### 📝 الوظيفة:
الراكب يغير صورته

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
```

### 📥 البيانات المطلوبة (Body - form-data):
- **Key**: `profileImg`
- **Type**: File
- **Value**: اختار صورة من جهازك

⚠️ **مهم**: لازم تستخدم `form-data` مش `JSON` عشان ترفع صورة!

---

## 1️⃣6️⃣ تغيير رقم التليفون

### 📍 Endpoint:
```
PUT /api/v1/users/profile
```

### 📝 الوظيفة:
الراكب يغير رقم تليفونه (هيوصله كود تحقق على الرقم الجديد)

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json
```

### 📥 البيانات المطلوبة (Body):
```json
{
  "phone": "+201111111111"
}
```

### 📤 الرد المتوقع:
```json
{
  "status": "pending_verification",
  "message": "Phone number updated. OTP sent for verification."
}
```

---

## 1️⃣7️⃣ التحقق من الرقم الجديد

### 📍 Endpoint:
```
POST /api/v1/users/profile/verify-phone
```

### 📝 الوظيفة:
بعد ما الكود يوصل على الرقم الجديد، الراكب يدخله هنا عشان يتأكد

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json
```

### 📥 البيانات المطلوبة (Body):
```json
{
  "code": "123456"
}
```

---

# 📍 العناوين المحفوظة

## 1️⃣8️⃣ حفظ عنوان جديد

### 📍 Endpoint:
```
POST /api/v1/passenger/addresses
```

### 📝 الوظيفة:
الراكب يحفظ عنوان (البيت، الشغل، مكان مفضل) عشان يستخدمه بسرعة

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json
```

### 📥 البيانات المطلوبة (Body):
```json
{
  "label": "home",
  "address": "22 شارع الثورة، مدينة نصر",
  "latitude": 30.0444,
  "longitude": 31.2357
}
```

#### شرح الحقول:
- **`label`**: نوع العنوان (لازم يكون واحد من دول):
  - `home` = البيت
  - `work` = الشغل
  - `favorite` = مكان مفضل
- **`address`**: العنوان الكامل (نص)
- **`latitude`**: خط العرض 
- **`longitude`**: خط الطول

---

## 1️⃣9️⃣ حذف عنوان محفوظ

### 📍 Endpoint:
```
DELETE /api/v1/passenger/addresses/{label}
```

### 📝 الوظيفة:
الراكب يمسح عنوان محفوظ

مثال: لو عايز تمسح عنوان البيت:
```
DELETE /api/v1/passenger/addresses/home
```

---

# 🔔 الإشعارات

## 2️⃣0️⃣ عرض الإشعارات

### 📍 Endpoint:
```
GET /api/v1/passenger/notifications?page=1&limit=20
```

### 📝 الوظيفة:
يعرض كل الإشعارات (رحلات، عروض، رسائل)

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
```

### 📥 Parameters (في الـ URL):
- **`page`**: رقم الصفحة (1، 2، 3...)
- **`limit`**: عدد الإشعارات في الصفحة (مثلاً 20)

### 📤 الرد المتوقع:
```json
{
  "status": "success",
  "results": 15,
  "unreadCount": 3,
  "data": [
    {
      "_id": "674567890abcdef123456789",
      "type": "ride",
      "title": "رحلة مكتملة",
      "message": "تم إكمال رحلتك بنجاح",
      "isRead": false,
      "data": {
        "rideId": "674567890abcdef111111111"
      },
      "createdAt": "2025-11-27T07:00:00Z"
    },
    {
      "_id": "674567890abcdef987654321",
      "type": "promotion",
      "title": "عرض خاص",
      "message": "احصل على خصم 20% على رحلتك القادمة",
      "isRead": true,
      "data": {
        "voucherCode": "SAVE20"
      },
      "createdAt": "2025-11-26T10:00:00Z"
    }
  ]
}
```

#### شرح الرد:
- **`results`**: عدد الإشعارات في الصفحة دي
- **`unreadCount`**: عدد الإشعارات اللي مقروءتش
- **`data`**: قائمة الإشعارات
  - **`type`**: نوع الإشعار
    - `ride` = خاص بالرحلات
    - `payment` = خاص بالمدفوعات
    - `promotion` = عروض وخصومات
    - `system` = رسائل من النظام
  - **`title`**: عنوان الإشعار
  - **`message`**: نص الإشعار
  - **`isRead`**: هل الإشعار اتقرا؟
  - **`data`**: بيانات إضافية (مثلاً رقم الرحلة أو كود الخصم)

---

## 2️⃣1️⃣ تعليم إشعار كمقروء

### 📍 Endpoint:
```
PUT /api/v1/passenger/notifications/{notificationId}/read
```

### 📝 الوظيفة:
لما الراكب يفتح إشعار، يتعلم إنه اتقرا

---

## 2️⃣2️⃣ تعليم كل الإشعارات كمقروءة

### 📍 Endpoint:
```
PUT /api/v1/passenger/notifications/read-all
```

### 📝 الوظيفة:
يعلم كل الإشعارات دفعة واحدة إنها اتقرت

---

## 2️⃣3️⃣ حذف إشعار

### 📍 Endpoint:
```
DELETE /api/v1/passenger/notifications/{notificationId}
```

### 📝 الوظيفة:
الراكب يمسح إشعار معين

---

# 🚗 تطبيق السواق

## 2️⃣4️⃣ تسجيل دخول السواق

### 📍 Endpoint:
```
POST /api/v1/auth/driver/phone
POST /api/v1/auth/driver/verify-otp
```

### 📝 الوظيفة:
نفس تسجيل دخول الراكب، بس للسواق

⚠️ **الفرق**: السواق بيسجل دخول من `/driver/phone` مش `/rider/phone`

---

## 2️⃣5️⃣ رفع رخصة القيادة

### 📍 Endpoint:
```
POST /api/v1/driver/license-front
POST /api/v1/driver/license-back
```

### 📝 الوظيفة:
السواق الجديد يرفع صورة رخصة القيادة (الوجهين)

### 🔑 Headers المطلوبة:
```
Authorization: Bearer YOUR_TOKEN
```

### 📥 البيانات المطلوبة (Body - form-data):
- **Key**: `licenseFront` (للوجه الأمامي) أو `licenseBack` (للوجه الخلفي)
- **Type**: File
- **Value**: صورة الرخصة

---

## 2️⃣6️⃣ رفع رخصة العربية

### 📍 Endpoint:
```
POST /api/v1/driver/car-registration
```

### 📝 الوظيفة:
السواق يرفع صورة رخصة العربية (الوجهين)

### 📥 البيانات المطلوبة (Body - form-data):
- **Key**: `carRegFront` (الوجه الأمامي)
- **Type**: File
- **Key**: `carRegBack` (الوجه الخلفي)
- **Type**: File

---

## 2️⃣7️⃣ رفع البطاقة الشخصية

### 📍 Endpoint:
```
POST /api/v1/driver/national-id
```

### 📝 الوظيفة:
السواق يرفع صورة البطاقة (الوجهين)

---

## 2️⃣8️⃣ رفع صور العربية

### 📍 Endpoint:
```
POST /api/v1/driver/car-photos
```

### 📝 الوظيفة:
السواق يرفع صور للعربية من برة (حتى 5 صور)

### 📥 البيانات المطلوبة (Body - form-data):
- **Key**: `carPhotos` (نفس الاسم لكل الصور)
- **Type**: File (اختار أكتر من ملف)

---

## 2️⃣9️⃣ إرسال للمراجعة

### 📍 Endpoint:
```
POST /api/v1/driver/submit-review
```

### 📝 الوظيفة:
بعد ما السواق يرفع كل المستندات، يضغط "إرسال للمراجعة" عشان الأدمن يشوفها

---

## 3️⃣0️⃣ Go Online / Offline

### 📍 Endpoint:
```
PUT /api/v1/driver/status/toggle
```

### 📝 الوظيفة:
السواق يفتح/يقفل استقبال الرحلات

⚠️ **مهم**: السواق لازم يكون `status: "active"` (الأدمن وافق عليه) عشان يقدر يستخدم ده

---

## 3️⃣1️⃣ تحديث موقع السواق

### 📍 Endpoint:
```
PUT /api/v1/driver/location
```

### 📝 الوظيفة:
السواق يبعت موقعه الحالي للسيرفر (بيتعمل أوتوماتيك كل شوية)

### 📥 البيانات المطلوبة (Body):
```json
{
  "latitude": 30.0444,
  "longitude": 31.2357
}
```

---

## 3️⃣2️⃣ عرض الأرباح

### 📍 Endpoint:
```
GET /api/v1/driver/earnings
```

### 📝 الوظيفة:
يعرض أرباح السواق (اليوم، الأسبوع، الإجمالي)

### 📤 الرد المتوقع:
```json
{
  "status": "success",
  "data": {
    "today": 150.50,
    "thisWeek": 850.00,
    "total": 5420.75
  }
}
```

#### شرح الرد:
- **`today`**: الأرباح النهاردة
- **`thisWeek`**: الأرباح الأسبوع ده
- **`total`**: إجمالي الأرباح من أول ما بدأ

---

## 3️⃣3️⃣ قبول رحلة

### 📍 Endpoint:
```
POST /api/v1/driver/rides/{rideId}/accept
```

### 📝 الوظيفة:
السواق يقبل رحلة جديدة

---

## 3️⃣4️⃣ بدء الرحلة

### 📍 Endpoint:
```
POST /api/v1/driver/rides/{rideId}/start
```

### 📝 الوظيفة:
السواق يبدأ الرحلة (بعد ما يستلم الراكب)

---

## 3️⃣5️⃣ إنهاء الرحلة

### 📍 Endpoint:
```
POST /api/v1/driver/rides/{rideId}/complete
```

### 📝 الوظيفة:
السواق ينهي الرحلة (بعد ما يوصل الراكب)

---

# 📝 ملاحظات عامة

## 🔑 الـ Headers:
في كل طلب (ماعدا تسجيل الدخول)، لازم تحط:
```
Authorization: Bearer YOUR_TOKEN
```

## 📥 أنواع الـ Body:
- **JSON**: لما تبعت بيانات نصية (أرقام، نصوص)
  - لازم تحط Header: `Content-Type: application/json`
- **form-data**: لما تبعت صور أو ملفات
  - متحطش `Content-Type` خالص، Postman هيحطها أوتوماتيك

## 🌐 الـ Base URL:
كل الـ endpoints تبدأ بـ:
```
http://213.210.20.206:9000
```

مثال كامل:
```
http://213.210.20.206:9000/api/v1/passenger/rides/request
```

جاهز للاختبار! 🚀
